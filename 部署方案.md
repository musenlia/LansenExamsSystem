# 学习培训考试管理系统 V2.0 - 使用说明

## 1. 系统概述

学习培训考试管理系统是一款基于 FastAPI + React 构建的在线考试平台，支持题库管理、考试发布、实时监控、成绩统计等功能。

| 项目 | 内容 |
|------|------|
| 系统名称 | 学习培训考试管理系统 |
| 版本号 | V2.0 |
| 研发作者 | lansen |
| 授权方式 | 开源发布，仅供学习与教学使用 |
| 发布日期 | 2026年6月2日 |

---

## 2. 环境要求

| 配置项 | 最低配置 | 推荐配置 |
|--------|----------|----------|
| CPU | 双核 2.0GHz | 四核 2.5GHz |
| 内存 | 2GB | 4GB |
| 磁盘空间 | 2GB | 4GB |
| 操作系统 | Windows 10/11 (64位) | Windows 10/11 (64位) |
| Python | 3.13+ | 3.14 |

---

## 3. 快速开始

### 3.1 启动服务

双击运行项目根目录下的 **`run.bat`**，脚本将自动完成：
- 检测并初始化数据库（首次运行）
- 创建默认管理员账号
- 启动 Web 服务器
- 自动打开浏览器访问系统

> 手动启动：在 `app` 目录下执行 `python -m uvicorn main:app --host 0.0.0.0 --port 8000`

### 3.2 停止服务

**方式一**：双击运行 **`stop_server.bat`** 或执行 **`python stop_server.py`**

**方式二**：在命令行窗口按 `Ctrl + C`

### 3.3 登录系统

浏览器访问：**`http://localhost:8000`**

| 角色 | 账号 | 密码 |
|------|------|------|
| 管理员 | `admin` | `admin123` |

> ⚠️ 首次登录后请立即修改默认密码

---

## 4. 目录结构

```
ExamsSystem_2.0/
├── run.bat                  # 启动脚本
├── stop_server.bat          # 停止脚本
├── stop_server.py           # 停止脚本 (Python版)
├── app/                     # 应用代码
│   ├── main.py              # 服务入口
│   ├── config.yaml          # 配置文件
│   ├── database.py          # 数据库配置
│   ├── init_data.py         # 数据库初始化
│   ├── routers/             # API 路由 (10个模块)
│   ├── models/              # 数据模型
│   ├── schemas/             # Pydantic 模型
│   ├── services/            # 业务逻辑层
│   ├── middleware/          # 中间件 (认证/日志)
│   ├── utils/               # 工具函数
│   └── static/              # 前端页面
│       ├── index.html       # SPA 入口
│       ├── admin.html       # 考试管理中心
│       └── assets/          # 前端资源 (JS/CSS)
├── data/                    # 数据库文件
│   └── exam_system.db
└── migrations/              # 数据库迁移脚本
```

---

## 5. 功能模块

### 5.1 管理员端

| 模块 | 功能说明 |
|------|----------|
| 📊 仪表盘 | 系统概览、数据统计 |
| 👤 用户管理 | 用户增删改查、批量导入 |
| 🏫 班级管理 | 班级创建与维护 |
| 📂 分类管理 | 题库分类管理 |
| 📝 题库管理 | 试题录入、编辑、导入 |
| 📋 考试管理 | 创建考试、组卷、发布/暂停/终止 |
| 📈 成绩总览 | 成绩统计与分析 |
| ⚙️ 系统设置 | 系统参数配置 |

### 5.2 考试管理中心（考试调整）

通过管理员左侧菜单"**考试调整**"进入，提供以下功能：

| 功能 | 说明 |
|------|------|
| 🔄 重考 | 将已交卷考生重置为未开始，清空答题记录和分数 |
| ▶️ 续考 | 恢复已交卷考生为答题中，**保留**答题记录 |
| ⏱️ 补时 | 为正在答题的考生增加额外考试时间（分钟） |
| ✅ 强制交卷 | 强制提交考生试卷，结束考试 |
| 🔁 重新激活 | 将已结束/已归档的考试重新激活，重置所有考生会话 |

### 5.3 教师端

| 模块 | 功能说明 |
|------|----------|
| 📈 成绩总览 | 查看所管班级考试成绩 |
| 👁️ 实时监考 | 实时查看考生状态 |

### 5.4 考生端

| 模块 | 功能说明 |
|------|----------|
| 📋 我的考试 | 查看已发布的考试列表 |
| ✍️ 在线答题 | 进入考试作答 |
| 📊 成绩查询 | 查看考试成绩 |

---

## 6. API 接口

系统提供 10 个 API 路由模块：

| 路由前缀 | 标签 | 说明 |
|----------|------|------|
| `/api/auth` | 认证 | 登录、登出、获取用户信息、修改密码 |
| `/api/users` | 用户管理 | 用户 CRUD、批量导入 |
| `/api/classes` | 班级管理 | 班级 CRUD |
| `/api/categories` | 分类管理 | 题库分类 CRUD |
| `/api/questions` | 题库管理 | 试题 CRUD |
| `/api/exams` | 考试管理 | 考试 CRUD、发布/暂停/恢复/终止/重新激活 |
| `/api/sessions` | 考试会话 | 考生会话管理 |
| `/api/monitor` | 实时监控 | 监控总览、重考/续考/补时/强制交卷 |
| `/api/scores` | 成绩管理 | 成绩查询与统计 |
| `/api/system` | 系统设置 | 系统配置、关于信息 |

API 文档地址：
- Swagger UI：`http://localhost:8000/api/docs`
- ReDoc：`http://localhost:8000/api/redoc`

---

## 7. 配置文件

编辑 `app/config.yaml` 可修改系统配置：

```yaml
database:
  url: sqlite:///../data/exam_system.db   # 数据库路径

server:
  host: 0.0.0.0        # 监听地址
  port: 8000            # 监听端口
  debug: false          # 调试模式

jwt:
  secret_key: ...       # JWT 密钥
  algorithm: HS256      # 签名算法
  access_token_expire_minutes: 1440  # Token 有效期 (分钟)

upload:
  max_size_mb: 10       # 上传文件最大大小
  allowed_extensions: [.xlsx, .xls, .doc, .docx, .pdf, .jpg, .png]
```

---

## 8. 数据库管理

### 8.1 备份数据库

```bash
# 直接复制文件
copy data\exam_system.db data\exam_system_backup.db
```

### 8.2 恢复数据库

```bash
# 停止服务后执行
copy data\exam_system_backup.db data\exam_system.db
```

---

## 9. 安全建议

1. **修改默认密码**：首次登录后立即修改管理员密码
2. **更换 JWT 密钥**：修改 `config.yaml` 中的 `secret_key`
3. **限制访问来源**：生产环境修改 CORS 配置和防火墙规则
4. **定期备份**：建议每日备份数据库文件
5. **日志监控**：关注系统日志，及时发现异常

---

## 10. 故障排除

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 端口被占用 | 8000 端口已被其他程序使用 | 运行 `stop_server.bat` 或修改 `config.yaml` 端口号 |
| 页面空白 | 浏览器缓存问题 | 按 `Ctrl + F5` 强制刷新 |
| 数据库文件损坏 | 异常关机导致 | 从备份恢复数据库 |
| 依赖缺失 | Python 环境不完整 | 确保 Python 3.13+ 并安装所需依赖 |
| 切屏警告 | 考试中切换了浏览器标签页 | 这是正常的防作弊机制 |

---

## 11. 更新日志

### V2.0 (2026年6月)

- ✅ 全新 React SPA 前端界面
- ✅ 考试管理中心（重考/续考/补时/强制交卷）
- ✅ 考试重新激活功能（含会话重置）
- ✅ 实时监控与 WebSocket 推送
- ✅ 防作弊机制（切屏检测、全屏保持）
- ✅ 考题蒙板隐私保护（灰度优化）
- ✅ 一键启动/停止脚本

---

**文档版本**: V2.0  
**更新日期**: 2026年6月4日  
**适用版本**: 考试系统 V2.0
