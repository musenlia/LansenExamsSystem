# 学习培训考试管理系统 V2.0 - 研发设计框架说明

## 1. 系统架构

### 1.1 技术栈

| 分类 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 语言 | Python | 3.13+ | 后端开发语言 |
| 框架 | FastAPI | 0.110+ | Web API框架 |
| ORM | SQLAlchemy | 2.0+ | 数据库ORM |
| 数据库 | SQLite | 3.x | 轻量级数据库 |
| 认证 | JWT (python-jose) | - | 身份认证 |
| 序列化 | Pydantic | 2.0+ | 数据验证 |
| 服务器 | Uvicorn | 0.25+ | ASGI服务器 |
| 前端 | React (Vite) + Ant Design | - | SPA 单页应用 |
| 实时通信 | WebSocket | - | 监控数据推送 |

### 1.2 架构设计

- **架构风格**: 集成式单体应用 (Integrated Monolith)
- **设计模式**: MVC + Service Pattern
- **通信方式**: RESTful API + WebSocket

### 1.3 模块划分

```
app/
├── main.py          # 服务入口，路由注册、中间件配置
├── database.py      # 数据库连接配置
├── config.yaml      # 系统配置文件
├── init_data.py     # 数据库初始化（默认数据）
├── routers/         # API路由层（Controller）— 10个模块
├── services/        # 业务逻辑层（Service）
├── models/          # 数据库模型（Model）
├── schemas/         # 数据传输模型（DTO）
├── utils/           # 工具函数
├── middleware/      # 中间件（认证、日志）
└── static/          # 静态资源（前端 SPA + 管理页面）
```

## 2. 目录结构

```
ExamsSystem_2.0/
├── run.bat                 # 启动脚本
├── stop_server.bat         # 停止脚本
├── stop_server.py          # 停止脚本 (Python)
├── app/                    # 应用代码
│   ├── routers/            # API路由
│   │   ├── auth.py         # 认证相关API
│   │   ├── user.py         # 用户管理API
│   │   ├── class_.py       # 班级管理API
│   │   ├── category.py     # 分类管理API
│   │   ├── question.py     # 题目管理API
│   │   ├── exam.py         # 考试管理API（含 reactivate）
│   │   ├── session.py      # 会话管理API
│   │   ├── monitor.py      # 监控管理API（重考/续考/补时/强制交卷）
│   │   ├── score.py        # 成绩管理API
│   │   └── system.py       # 系统配置API
│   ├── services/           # 业务服务
│   │   ├── auth_service.py
│   │   ├── class_service.py
│   │   ├── exam_service.py # 考试服务（含 reactivate 含 session 重置）
│   │   ├── session_service.py
│   │   ├── monitor_service.py
│   │   ├── score_service.py
│   │   └── user_service.py
│   ├── models/             # 数据库模型
│   │   ├── user.py
│   │   ├── class_.py
│   │   ├── category.py
│   │   ├── question.py
│   │   ├── exam.py         # 考试模型（6种状态）
│   │   ├── session.py      # 会话模型（5种状态 + extra_time）
│   │   ├── answer.py
│   │   ├── monitor_log.py
│   │   ├── operation_log.py
│   │   └── system_config.py
│   ├── schemas/            # Pydantic模型
│   ├── utils/              # 工具函数
│   │   ├── security.py     # 安全工具（JWT、密码哈希）
│   │   ├── grading.py      # 阅卷工具
│   │   ├── shuffle.py      # 乱序工具
│   │   └── excel_helper.py # Excel处理
│   ├── middleware/          # 中间件
│   │   ├── auth_middleware.py  # JWT 认证中间件
│   │   └── logging_middleware.py # 操作日志中间件
│   ├── static/             # 前端页面
│   │   ├── index.html      # SPA 入口
│   │   ├── admin.html      # 考试管理中心（独立页面）
│   │   └── assets/         # React 构建产物
│   ├── main.py
│   ├── database.py
│   └── config.yaml
├── data/                   # 数据库文件
│   └── exam_system.db
├── migrations/             # 迁移脚本
├── 部署方案.md              # 部署文档
├── 使用说明.txt             # 快速入门
├── API文档_重考续考补时.md   # 考试调整 API 文档
├── 研发设计框架说明.md       # 本文件
└── 软件声明.md              # 版权声明
```

## 3. 核心模块说明

### 3.1 路由层 (routers/)

负责处理HTTP请求，参数校验，调用Service层，返回响应。

**设计原则**:
- 每个资源对应一个路由文件
- 路由函数只负责请求转发，不包含业务逻辑
- 使用Pydantic模型进行请求/响应校验

### 3.2 服务层 (services/)

实现核心业务逻辑，调用模型层进行数据操作。

**设计原则**:
- 事务管理在Service层实现
- 复杂业务逻辑封装为Service方法
- Service之间可相互调用

### 3.3 模型层 (models/)

定义数据库表结构，使用SQLAlchemy 2.0 ORM。

**设计原则**:
- 使用声明式语法 + Mapped 类型注解
- 每个模型对应一个数据库表
- 模型包含 `to_dict()` 方法用于数据转换

### 3.4 Schema层 (schemas/)

定义API请求/响应的数据结构，使用Pydantic。

**设计原则**:
- 请求模型：Create/Update
- 响应模型：Response/DetailResponse
- 使用Field进行字段约束和描述

## 4. 数据库设计

### 4.1 核心表结构

| 表名 | 说明 | 核心字段 |
|------|------|----------|
| users | 用户表 | id, username, name, role, status |
| classes | 班级表 | id, name, description |
| categories | 分类表 | id, name, parent_id |
| questions | 题目表 | id, type, content, answer, score |
| exams | 考试表 | id, name, duration, status, start_time, shuffle_question, shuffle_option, allow_early_submit |
| exam_sessions | 考试会话表 | id, exam_id, user_id, status, is_active, extra_time, time_used, total_score |
| answers | 答题记录表 | id, session_id, question_id, user_answer, is_correct, is_flagged |
| monitor_logs | 监控日志表 | id, session_id, event_type, detail |
| operation_logs | 操作日志表 | id, user_id, action, detail |

### 4.2 考试状态流转

```
draft ──发布──> published ──自动──> ongoing ──暂停──> paused ──恢复──> ongoing
                  │                     │
                  │                     ├──终止──> ended ──归档──> archived
                  │                     │            │
                  │                     └──自动到期──> ended ────────┘
                  │                                   │
                  └──终止──────────────────────────> ended
                                                       │
                              archived ──重新激活──> published
```

### 4.3 会话状态流转

```
not_started ──进入考试──> in_progress ──正常交卷──> submitted
                              │
                              ├──强制交卷──> force_submitted
                              ├──超时自动──> auto_submitted
                              │
              submitted/force_submitted/auto_submitted
                              │
                              ├──重考(reset)──> not_started (清空记录)
                              └──续考(resume)──> in_progress (保留记录)
```

### 4.4 布尔字段规范

| 表名 | 字段名 | 类型 | 默认值 | 说明 |
|------|--------|------|--------|------|
| exams | shuffle_question | BOOLEAN | False | 是否乱序题目 |
| exams | shuffle_option | BOOLEAN | False | 是否乱序选项 |
| exams | allow_early_submit | BOOLEAN | True | 是否允许提前交卷 |
| exam_sessions | is_active | BOOLEAN | True | 是否活跃 |
| answers | is_correct | BOOLEAN | NULL | 是否正确 |
| answers | is_flagged | BOOLEAN | False | 是否标记 |

## 5. API设计规范

### 5.1 命名规范

- **URL**: 使用小写字母，单词间用连字符分隔
- **资源**: 使用复数形式
- **动作**: 使用HTTP方法表示操作类型

### 5.2 统一响应格式

```json
{
  "code": 0,
  "data": {},
  "message": "success"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 状态码，0表示成功 |
| data | any | 响应数据 |
| message | string | 提示信息 |

### 5.3 认证方式

所有 API（除登录外）需要在请求头中携带 JWT Token：
```
Authorization: Bearer <token>
```

## 6. 安全设计

### 6.1 认证机制

- 使用 JWT Token 进行身份认证
- Token有效期 1440 分钟（24小时）
- 支持密码修改功能

### 6.2 权限控制

| 角色 | 权限 |
|------|------|
| admin | 所有权限（含考试调整、重新激活） |
| teacher | 班级管理、考试管理、成绩查看、实时监考 |
| student | 参加考试、查看成绩 |

### 6.3 数据安全

- 密码使用 BCrypt 加密存储
- SQL注入防护（ORM参数化查询）
- 防作弊：切屏检测、全屏保持、考题蒙板

## 7. 日志与监控

### 7.1 操作日志

记录管理员的所有操作：操作时间、操作用户、操作动作、操作对象

### 7.2 监控日志

记录考试过程中的异常事件：切屏事件、多端登录、自动提交、强制提交

### 7.3 WebSocket 实时推送

监控端通过 WebSocket 实时接收考生状态更新、切屏告警等事件。

## 8. 开发规范

### 8.1 代码风格

- 遵循 PEP 8 规范
- 使用类型注解
- 函数/方法注释使用 docstring

### 8.2 提交规范

```
<类型>: <描述>
```

**类型说明**:
- feat: 新增功能
- fix: 修复bug
- docs: 更新文档
- refactor: 重构代码
- test: 添加测试
- chore: 构建/工具更新

---

**文档版本**: V2.0  
**更新日期**: 2026年6月4日  
**适用版本**: 考试系统 V2.0
